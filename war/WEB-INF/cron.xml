<?xml version="1.0" encoding="UTF-8"?>
<cronentries>
  <cron>
    <url>/service/appointmentcleanup?op=cleanup</url>
    <description>Cleans up reserved appointments</description>
    <schedule>every 5 minutes</schedule>
  </cron>
  <!-- Uncomment this job for continuous mark as current
  <cron>
    <url>/service/appointmentcleanup?op=startMarkAllCurrent</url>
    <description>Mark future appointments as current</description>
    <schedule>every 6 hours</schedule>
  </cron> -->
</cronentries>